name: calendar_views_example
version: 0.5.0
description: Examples of calendar_views library.

environment:
  sdk: '>=2.0.0 <3.0.0'

dependencies:
  flutter:
    sdk: flutter
  meta: ^1.1.6
  calendar_views:
    path: ../

dev_dependencies:
  flutter_test:
    sdk: flutter

flutter:
  uses-material-design: true
