name: calendar_views
version: 0.5.4
description: Collection of customisable calendar related widgets for Flutter.
homepage: https://github.com/cenkingunlugu/calendar_views

dependencies:
  flutter:
    sdk: flutter
  meta: ^1.1.5
  quiver: ^3.0.0

dev_dependencies:
  flutter_test:
    sdk: flutter

environment:
  sdk: ">=2.16.0 <3.0.0"
